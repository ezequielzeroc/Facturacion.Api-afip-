import React from 'react';
import {Container} from 'semantic-ui-react'
 const ErrorPage = ()=>(
  <Container  style={{ marginTop: '7em' }}>
  <div>
    <h1>Error</h1>
    <p>Ha ocurrido un error crítico en la plataforma.</p>
  </div>
  </Container>
);
export default ErrorPage;
